/**
 * @fileoverview Re-export of React Native's useColorScheme hook
 * Provides the current color scheme (light/dark) from the system
 * 
 * @example
 * import { useColorScheme } from '@/hooks/useColorScheme';
 * 
 * const colorScheme = useColorScheme();
 * console.log(colorScheme); // 'light' | 'dark' | null
 */

export { useColorScheme } from 'react-native';
